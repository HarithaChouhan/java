improt java.until.Scanner;//
#include<stdio.h>
class AdditionExample
{
static void addation(int num1,int num2);
{
System.out.println(num1+num2);
}
Scannerscaner=new
Scanner(System.in);
addition(scanner.nextlnint(),scanner.nextint());
addition(scanner.nextlnint(),scanner.nextint());
addition(scanner.nextlnint(),scanner.nextint());
}
}